package movie_reservation.types;

public enum SeatState { // state of the seats
    // available: the seat can be reserved
    // not_available: the seat has been taken or can not be used
    AVAILABLE, NOT_AVAILABLE
}